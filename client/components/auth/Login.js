import React from 'react';

export default class Login extends React.Component {
    static defaultProps = {
        foo: 'bar'
    };

    state = {
        foo: 'bar'
    };

    render() {
        return (
            <div>

            </div>
        );
    }

}